{
  "scripts": {
    "setup": "pnpm install && ln -sf \"$CONDUCTOR_ROOT_PATH/.env.local\" .env.local && ln -sf \"$CONDUCTOR_ROOT_PATH/.env.local\" apps/web/.env.local",
    "run": "PORT=$CONDUCTOR_PORT pnpm dev"
  },
  "runScriptMode": "nonconcurrent"
}
